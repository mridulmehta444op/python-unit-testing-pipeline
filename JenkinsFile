pipeline {
    agent any

    stages {

        stage('Check Python') {
            steps {
                bat 'python --version'
                bat 'py --version'
            }
        }

        stage('Check Pip') {
            steps {
                bat 'python -m pip --version'
            }
        }
    }
}